import * as ActionTypes from '../actions';
import _ from 'lodash';

const INITIAL_STATE = {};

export const shop = (state = INITIAL_STATE, action) => {
  const { type } = action;
  switch (type) {

    default:
      return state;
  }
};
